# Decision Journal

### 2026-04-18 — Plan-schema operator-decision flags live in a Section 4 addendum, not a new section

- **Context:** First real invocation of `/cleanup-worktree` produced a plan with two operator decisions that did not fit the Section 4 hard-gate schema: (a) whether to commit the workflow-template copies of `qc-pass.md` / `refinement-pass.md` as local forks or convert them to symlinks, and (b) whether to commit or delete the already-consumed `inbox/worktree-cleanup-brief.md`. Neither decision involves an irreversible operation requiring a named confirmation phrase. The first QC pass correctly flagged a BLOCKING schema violation: the plan had populated a hard-gate block (G1) inside Section 4 while simultaneously declaring "no hard gates required." Triage surfaced the first-class alternative of *hoisting* operator-decision flags out of Section 4 entirely into a sibling section, versus keeping them as a *Section 4 addendum* with a clearly-labeled non-hard-gate sub-schema.
- **Decision:** Preserve the skill's 8-section plan schema by restructuring Section 4 into (i) "Hard gates in this plan: NONE" lead paragraph enumerating the classes of irreversible operation that are absent, followed by (ii) a clearly-labeled "Section 4 addendum — Operator-decision flags (non-hard-gate sub-schema)" with its own defined fields: `question`, `paths affected`, `operator options`, `default option`, `abort / revision scope`, `justification`. Each addendum block explicitly declares that it is NOT part of the Section 4 hard-gate inventory schema defined in `execution-protocol.md` § 7. Operator-decision flags therefore live inside Section 4 (as an addendum) rather than as a ninth section or hoisted sibling.
- **Rationale:** The skill's invocation contract (`SKILL.md` → Invocation Contract) requires the eight sections defined in `execution-protocol.md` → "Plan file schema" precisely because two QC subagents and one triage subagent read the plan as a stable contract. Introducing a ninth section unilaterally in the first real invocation would silently change the contract in a way subagents cannot predict. An addendum inside Section 4 with an explicit "NOT part of § 7 schema" label is a smaller, self-documenting extension that preserves the contract while making the non-hard-gate nature unambiguous to a second reader. The cost is mild verbosity in Section 4; the benefit is every plan produced under the unmodified skill will parse the same way.
- **Alternatives considered:** (a) Hoist flags into a new "Section 4a — Operator decisions" sibling section (rejected — breaks the load-bearing 8-section contract without improving the skill; first-class alternative from triage). (b) Move flags into Section 3's per-path classification table with a new `operator-decision` decision category (rejected — dilutes the seven-decision taxonomy and makes path-level classification carry plan-level operator-decision semantics). (c) Embed flags only in Section 8 as "revision-required branches" (rejected — Section 8 is revision history, not a pre-commit gate surface, and subagents would not read it with the right semantics). (d) Skip the schema fidelity entirely and put flags wherever convenient (rejected — was the original plan's path and the first QC pass caught it as BLOCKING).
- **Follow-up:** Logged as `/improve-skill worktree-cleanup-investigator` item #6 in the session plan — if the addendum pattern proves useful across multiple cleanups, consider formalizing it in `execution-protocol.md` → "Plan file schema" so future plans have a defined home for operator-decision flags rather than inventing one. Do NOT formalize preemptively — wait for pattern recurrence.

---

### 2026-04-18 — Script / taxonomy / spec-conflict discoveries during /cleanup-worktree are logged for later /improve-skill, not fixed opportunistically in the same session

- **Context:** The first real invocation of the `worktree-cleanup-investigator` skill surfaced four categories of defect in the skill's own tooling during investigation and triage: (1) `find-template.sh` has a `workflows/` prefix pattern-matching blind spot that returns false-negative NO_TEMPLATE_FOUND for any workflow-internal path; (2) `find-template.sh` also fails on D-status inputs because its `[ ! -f "$INPUT_PATH" ]` precondition rejects absent files with exit 3; (3) `decision-taxonomy.md` § 3 (`delete`) mandates hard-gate protocol with no carve-out for pre-existing filesystem deletions that the current session only stages at the index level; (4) `execution-protocol.md` § 11 and `ai-resources/CLAUDE.md` Git Rules define different commit-prefix vocabularies (`chore:`/`docs:`/`plan:`/`session:`/`fix:`/`new:` vs `new:`/`update:`/`batch:`/`fix:`) and neither defines `workflow:`. A fifth discovery — `ai-resources/CLAUDE.md` is silent on post-consumption inbox brief disposition — surfaced as the justification for soft flag G2.
- **Decision:** Log all five issues (plus the Section 4 addendum pattern from the companion decision above) as `/improve-skill worktree-cleanup-investigator` follow-ups in the plan's Section 8, and do NOT attempt to fix any of them within the same `/cleanup-worktree` session. The current session commits ship with explicit workarounds (manual `diff` in place of find-template.sh for Blind spot A, manual `ls` + `git show` in place of find-template.sh for Blind spot B, reclassification of D-status path from decision 3 to decision 1 in place of taxonomy § 3 fix, adopted CLAUDE.md prefix vocabulary as the authoritative choice, soft flag G2 in place of a documented convention) that are load-bearing only for this plan and will be superseded by the proper skill fixes in a dedicated session.
- **Rationale:** Opportunistic mid-session fixes to the cleanup skill would (a) scope-creep a worktree cleanup into a skill-improvement session, (b) consume the already-expensive cleanup-protocol token budget on unrelated artifacts, (c) require a separate `/improve-skill` pipeline run with its own QC + evaluation framework application that cannot be short-cut, and (d) mix skill-modification commits with cleanup-content commits in the same `git log` entry, defeating the topical-commit-split principle the cleanup skill enforces. Keeping the skill fix out of band preserves each session's scope contract and lets the operator budget `/improve-skill` runs explicitly.
- **Alternatives considered:** (a) Fix find-template.sh blind spots inline because both are one-line patches (rejected — still requires running the ai-resource-builder evaluation framework and QC pipeline per `ai-resources/CLAUDE.md` Development Workflow; the "one-line" part is misleading). (b) Edit `ai-resources/CLAUDE.md` inline to document the inbox post-consumption convention (rejected — governance-type edit that deserves operator discussion and a dedicated `/update-claude-md` run, not a drive-by edit during cleanup). (c) Defer the follow-ups to an open-ended "someday" list outside the repo (rejected — would lose the specific evidence chain discovered during investigation; the plan file is the right place because it will be read by the `/improve-skill` session as input). (d) Downgrade the current cleanup's safety by skipping the second QC pass to save tokens and using the saved budget to patch the skill (rejected — the second QC pass is load-bearing per the skill's own Bias Counters section and was the pass that caught the Row 12 line-count off-by-one).
- **Follow-up:** Next dedicated `/improve-skill worktree-cleanup-investigator` session should read `~/.claude/plans/noble-dancing-wall.md` Section 8 → `/improve-skill follow-ups` as the canonical input. The six items are sequenced by locus (script patches 1+2, taxonomy 3, doc reconciliation 4+5, schema formalization 6) and can be batched accordingly.

---

### 2026-04-17 — prose-compliance-qc: describe both Sequential and Merged operating modes explicitly, not just the historical sequential hand-off
- **Context:** The 2026-04-17 three-way split of `/produce-prose` into `/produce-architecture`, `/produce-prose-draft`, and `/produce-formatting` left `prose-compliance-qc` with three stale self-descriptions: the Position reference (line 18) pointed at the deleted Phase 4 of the monolithic command; the Role paragraph (line 16) said "Assume the prose has already been through chapter-prose-reviewer and any resulting fixes have been applied" — a sequential hand-off assumption that no longer holds inside `/produce-prose-draft` Phase 3, where this skill and `chapter-prose-reviewer` now run in the same subagent call against the same unmodified prose; the Failure Behavior entry for pre-refinement drafts (line 308) flagged pre-refinement input as an error even though pre-refinement is the expected input in the merged-pass mode. The workspace rule routes all skill edits through `/improve-skill`, and the operator's 2026-04-17 decisions.md entry on the split specifically flagged this follow-up.
- **Decision:** Update the skill to describe two operating modes explicitly — Sequential mode (standalone invocation, post-refinement input) and Merged mode (inside `/produce-prose-draft` Phase 3, running simultaneously with `chapter-prose-reviewer` against unmodified prose). Caller names the mode at invocation. Default is Sequential when unspecified. Merged mode treats the diagnostic-review findings as pending fixes and does not double-count issues the reviewer has already caught. Apply the fix across all three staleness points in a single commit, not just the line-18 Position reference the follow-up was originally scoped to. YAML description also updated so the trigger text reflects both modes.
- **Rationale:** The command-level sequencing instruction in `/produce-prose-draft` Phase 3 already tells the merged subagent how to reconcile the two skills' findings. But relying on command-level instructions to override skill-level self-descriptions is fragile — future maintainers reading only the skill form an incorrect mental model, and the subagent has to resolve a conflict between the skill's stated assumption and the command's instruction. Making the skill self-consistent eliminates the conflict and documents the merged-pass pattern in its canonical home. The cost is ~30 words of additional prose; the benefit is a skill that is self-describing without requiring the caller's context.
- **Alternatives considered:** (a) Fix only the line-18 Position reference and leave lines 16 and 308 for a future session — rejected because running the `/improve-skill` pipeline three times on the same skill for what is fundamentally one coherent cleanup is wasteful, and lines 16 and 308 are behavioral-assumption drift, not cosmetic drift. (b) Direct edit outside `/improve-skill` — rejected by the workspace rule and the operator's 2026-04-17 decision that cross-reference cleanup must route through `/improve-skill`. (c) Remove Sequential mode entirely and describe only the Merged mode — rejected because the skill is usable standalone (outside the pipeline) and downstream deployments may invoke it that way.

---

### 2026-04-17 — ai-prose-decontamination: integrate five new sub-patterns into the existing 4-pass structure, not as a new Pass 5; skill's output-path contract becomes caller-owned
- **Context:** The 2026-04-17 buy-side outside-read feedback on Doc 2 v1.0 surfaced five AI-tell patterns that the `ai-prose-decontamination` skill did not explicitly cover: contrast-template overuse, abstract-noun stacking, pseudo-maxim repetition, pivot closings, and Flagged-Word Registry application. The operator's improvement-log entry named each pattern and proposed candidate placements ("under Pass 1 or as a new Pass" for contrast-template; "under Pass 2 or Pass 4" for pivot closings; etc.). Independently, the Step 4 evaluator subagent flagged the skill's existing "Write Output" directive — "overwrite the input file" — as conflicting with the workspace-level Input File Handling rule (inputs are read-only) and with the operator's versioning convention (new version files rather than in-place overwrites).
- **Decision 1 — Structure:** Integrate five new sub-patterns into the existing 4-pass structure rather than adding a Pass 5. Each new pattern has a functional home inside an existing pass: contrast-template and abstract-noun stacking are vocabulary/ornament failures (Pass 1); Flagged-Word Registry is also a Pass 1 vocabulary authority; pivot closings are deletable-without-loss sentences (Pass 2); pseudo-maxim repetition is a rhythm failure (Pass 4). Sub-patterns are numbered (1a/1b/1c/2a/4a) with first-class H3 headings; Pass 3's existing three sub-patterns renumbered 3a/3b/3c during iteration for symmetry. The skill's frontmatter description retained the 4-pass framing and expanded only the parenthetical coverage list.
- **Decision 2 — Output contract:** The skill's default no longer overwrites the input file. The caller specifies the output path at invocation. When the caller passes the input path as the output (as `produce-prose-draft` Phase 5 does — the command owns the file-versioning contract for the pipeline), in-place overwrite happens explicitly. Standalone invocation defaults to a new versioned path (e.g., `{prose}-decontaminated.md` or `{prose}.v{n+1}.md`), preserving the pre-decontamination file for before/after comparison without going to git. A silent-overwrite-on-any-invocation is prohibited.
- **Rationale (Decision 1):** A new Pass 5 would have collected the five patterns as a grab-bag "control rhetorical habit" pass, weakening Pass 4's rhythm focus and creating category confusion between the new Pass 5 and the existing Pass 2 (which already governs deletable-without-loss sentences like pivot closings). Placing each pattern under its functional parent preserves the skill's existing mental model (four failure modes; passes address them in sequence) while elevating each new pattern to first-class visibility via numbered sub-headings. The change-log format was extended to carry sub-pattern counts, so downstream observability was not lost.
- **Rationale (Decision 2):** The skill was silently overwriting input files, which violated two workspace rules (Input File Handling, Version-file convention) and made pipeline vs. standalone behavior identical when they should differ. Pipelines that own the versioning contract can still overwrite by passing the input path explicitly. Standalone invocation now defaults to a safer behavior that preserves the pre-decontamination state. The caller-controlled output path keeps the skill domain-agnostic without requiring it to know the calling context.
- **Alternatives considered (Decision 1):** (a) Add a new Pass 5 consolidating the five patterns — rejected, weakens category coherence and creates overlap with Pass 2/Pass 4. (b) Add three new passes grouping the patterns differently (one for rhetorical habit, one for vocabulary, one for closings) — rejected, triples the passes for five sub-patterns, poor leverage. (c) Leave the five patterns as inline detection bullets in `produce-prose-draft` Phase 5 and skip the skill update — rejected by the workspace rule that skill-level procedural knowledge belongs in the skill, not in command prose.
- **Alternatives considered (Decision 2):** (a) Keep "overwrite input file" and flag the Input File Handling rule as an exception — rejected, silent exceptions erode the rule. (b) Force all invocations to write to a new path regardless of caller — rejected, pipeline commands legitimately own file versioning and need the option to overwrite. (c) Add a frontmatter flag (`overwrite: true|false`) — rejected, over-engineering; a path parameter already carries this information via the caller's choice.

---

### 2026-04-06 — Merged /repo-dd-deep into /repo-dd as depth levels
- **Context:** Built `/repo-dd-deep` as a standalone command for operational health review. Patrik asked whether the two commands could be combined for a "comprehensive audit."
- **Decision:** Extended `/repo-dd` with `$ARGUMENTS` depth control: (empty) = factual audit, "deep" = audit + judgment, "full" = audit + judgment + pipeline testing. Two separate report files preserve evidence/interpretation separation.
- **Rationale:** Eliminates two-session friction and the freshness dependency (deep review needed a recent audit as input). The separation principle is maintained at the report level, not the command level.
- **Alternatives considered:** (A) Combined command with separate reports (chosen), (B) Single combined report (rejected — blurs facts/judgment separation required by CLAUDE.md design principles).

### 2026-04-06 — Shared commands distributed via --add-dir, not copied into projects
- **Context:** Creating `/wrap-session` for ai-resources raised the question of how to make it available to all projects.
- **Decision:** Shared commands live in `ai-resources/.claude/commands/` and are available to all connected projects via `--add-dir`. Projects that need a specialized version override locally (local commands take precedence).
- **Rationale:** Single source of truth — updates propagate automatically. No sync burden. Research-workflow projects already override with their pipeline-specific version, proving the override pattern works.
- **Alternatives considered:** (a) Copy into each project at deploy time via `/deploy-workflow` — creates drift. (b) User-level commands in `~/.claude/commands/` — less portable, not version-controlled with the repo.

### 2026-04-06 — /repo-dd and /repo-review are separate commands
- **Context:** After running the first due diligence audit, Patrik wanted a command that also answers operational questions (feature criticality, context management health, friction synthesis, functional pipeline testing). These are judgment-based questions that don't fit the questionnaire's facts-only design.
- **Decision:** Keep `/repo-dd` as a structural audit (facts, no interpretation, comparable across runs). Build `/repo-review` separately for operational health assessment (judgment, recommendations, reads friction logs and tests pipelines).
- **Rationale:** Mixing factual audit with interpretive assessment would break the questionnaire's repeatability. A structural audit that says "this file is stale" is different from an operational review that says "this feature is your highest priority to fix." Different inputs, different outputs, different cadence.
- **Alternatives considered:** (a) Expand `/repo-dd` to include operational questions — rejected because it introduces judgment into a facts-only tool. (b) One command with two modes — adds complexity without benefit.

### 2026-04-06 — AUTO-FIX triage defaults to OPERATOR when ambiguous
- **Context:** The /repo-dd command includes a triage step that categorizes audit findings for fixing. The risk is Claude Code being too aggressive about what it auto-fixes.
- **Decision:** AUTO-FIX is strictly conservative: only unambiguous, single-file, no-cascade fixes. Everything else defaults to OPERATOR. The triage instruction explicitly says "when in doubt, classify as OPERATOR."
- **Rationale:** A conservative auto-fix list is more trustworthy than an ambitious one. Claude Code may misjudge whether a naming inconsistency is a simple rename or a cascading change. Better to surface more items for operator decision than to silently break references.
- **Alternatives considered:** (a) More permissive auto-fix with rollback — adds complexity, harder to verify. (b) No auto-fix at all — loses the convenience of fixing genuinely trivial items.

### 2026-04-06 — fund-triage-scanner stays as project-specific skill
- **Context:** Audit flagged `fund-triage-scanner` in nordic-pe as violating the "skills belong in ai-resources" rule.
- **Decision:** Keep it in the project. Document as intentional exception in nordic-pe CLAUDE.md.
- **Rationale:** The skill is single-use, tightly coupled to the nordic-pe triage pipeline, and not reusable across other projects. Graduating it would add clutter to the shared library with no benefit.
- **Alternatives considered:** (a) Graduate to ai-resources — rejected, no reuse case. (b) Delete it and inline the logic — rejected, it's a working skill that follows the standard format.

### 2026-04-06 — Canonical report-compliance-qc updated to match deployed copies
- **Context:** Audit found the canonical skill (ai-resources) had outdated single-file paths while buy-side and workflow template copies had correct section-specific paths. The copies were ahead of canonical.
- **Decision:** Update canonical to match the deployed copies. Replace buy-side physical copy with symlink to prevent future divergence.
- **Rationale:** The deployed copies had the correct paths for how the workflow actually works (section-scoped architecture files). The canonical version was simply never updated when the workflow evolved. Workflow template keeps its physical copy (needed for deploy-time copying) but is now in sync.

### 2026-04-06 — Exclusion-list approach for feature enrichment in project deployment
- **Context:** New features added to ai-resources (commands, agents, hooks) weren't reaching newly deployed projects because `/deploy-workflow` copies from a stale template snapshot.
- **Decision:** Use an exclusion list (repo-specific items blocked) rather than an inclusion list (shared items allowed). New features flow to projects by default.
- **Rationale:** Patrik constantly adds new features. An inclusion list requires manual updates every time. An exclusion list only needs updating when a new repo-specific tool is created, which is rare. The 9 excluded commands, pipeline agents, and 2 hooks are clearly ai-resources-only.
- **Alternatives considered:** (a) Inclusion list — rejected, requires constant maintenance. (b) Keep template manually updated — rejected, that's the discipline problem this fix solves. (c) Symlinks instead of copies — rejected, projects are standalone repos that may diverge intentionally.

### 2026-04-06 — Shared command drift check over dedicated sync command
- **Context:** Improvements made to shared commands in project workspaces (e.g., buy-side adding coaching data to wrap-session) don't automatically flow back to ai-resources. No mechanism existed to catch this drift.
- **Decision:** Add a lightweight drift check step to `/wrap-session` rather than building a dedicated `/sync-command` skill.
- **Rationale:** Piggybacks on an existing routine (session wrap) so it costs zero extra process. Only asks the question — doesn't force action. If this proves insufficient, a dedicated command can be built later.
- **Alternatives considered:** (a) Manual habit — rejected, relies on memory. (b) Dedicated `/sync-command` — deferred, over-engineering for a problem that may be fully solved by a prompt. (c) Automated diffing — rejected, too complex for the current volume of shared commands.

### 2026-04-11 — Bright-line check 1 exemption for AI prose decontamination
- **Context:** The produce-prose pipeline's bright-line rule requires operator approval for multi-paragraph changes. The new Phase 5c (AI prose decontamination) operates across the entire document by design — every invocation makes multi-paragraph changes. Requiring approval on check 1 would make every invocation a manual gate, defeating the automatic flow design.
- **Decision:** Exempt Phase 5c from bright-line check 1 (multi-paragraph scope). Checks 2 (analytical claim alteration) and 3 (sourced statement modification) remain active and trigger a PAUSE if violated.
- **Rationale:** The bright-line rule protects analytical content from unreviewed changes. Decontamination explicitly does not change analytical content — it changes surface-level voice patterns. The exemption preserves the rule's intent while allowing the pass to function. This is the first exemption to the bright-line rule in the workflow.
- **Alternatives considered:** (a) Full bright-line compliance — rejected, creates a mandatory gate on every invocation. (b) Raising the threshold (e.g., exempt only if <50 changes) — rejected, arbitrary threshold that still gates most invocations.

### 2026-04-13 — Codex second-opinion auditor: independent executor pattern, mechanical wrapper, strict ordering
- **Context:** Operator raised the blind-spot risk that every audit and QC pass in this workspace is executed by Claude (Opus 4.6). Subagent isolation gives fresh context but not a fresh model — two Claude instances evaluating the same artifact share training data and failure modes, so some gaps stay invisible across arbitrarily many review passes. Question: can a different model family serve as a genuine second opinion?
- **Decision:** Viability investigation concluded positively with a specific pattern. (1) Codex CLI (`codex exec`) runs the workspace's audit *framework files* natively — not Claude's subagent-dependent command wrappers. Framework files (`audits/questionnaire.md`, `skills/ai-resource-builder/references/evaluation-framework.md`) are model-agnostic; command files are not. (2) Strict ordering rule: Claude produces its audit first, Codex produces its audit second, never reverse. (3) Wrapper prompt from Claude to Codex must be mechanical — paths + schema only, no editorial framing — to preserve independence. (4) Park as inbox brief this session; pilot is a single command `/codex-dd` using the existing questionnaire, one real run, then decide whether to expand.
- **Rationale:** `codex exec` is a fresh-context agentic loop structurally equivalent to a Claude subagent, with stronger process-level isolation (no shared conversation state at all). The QC Independence Rule is satisfied by construction. Running against framework files rather than command wrappers avoids coupling to Claude Code's Agent-tool execution model, which Codex cannot replicate. The ordering rule matters because the value depends on each model forming its view without seeing the other's; reverse ordering silently breaks the pattern. The mechanical wrapper prompt matters because Claude's natural impulse to "help" by summarizing prior findings is exactly what contaminates Codex's independent evaluation. Starting with one command lets a single real run reveal format-divergence issues and value signal before committing to porting all six subagent-dependent audit commands.
- **Alternatives considered:** (a) Codex reviews Claude's report as a second-reviewer (rejected — two models reading the same summary share more signal than two models reading the same source material independently, and it introduces a cognitive anchor that defeats the independence goal). (b) Extend the existing `.claude/agents/execution-agent.md` HTTP pattern for GPT-5/Perplexity to cover Codex (rejected — Codex CLI is a multi-turn agentic process with its own tools, not a one-shot request/response call; forcing it through the HTTP pattern loses the value). (c) Build the `/codex-dd` pilot this session (rejected — operator preferred a conversational viability memo parked as a draft for future pickup, preserving optionality). (d) Port all six subagent-dependent audit commands at once (rejected — format divergence and value signal are unknown; a single pilot produces those answers cheaply). (e) Run Codex first, then Claude (rejected — Claude seeing Codex output first contaminates Claude's independence, which is a one-way door).

### 2026-04-13 — Reversed "additionalDirectories intentionally omitted" decision in /new-project
- **Context:** The `/new-project` pipeline's post-pipeline enrichment step previously kept the canonical permissions block scoped to `allow`/`deny` arrays only, with an explicit comment that `additionalDirectories` was "intentionally omitted because it is a user-level absolute path". In practice this meant newly scaffolded projects couldn't read through symlinks into `ai-resources/` — skills (under `ai-resources/skills/`) were invisible to Claude Code's per-project sandbox, and even the symlinked commands under the project's `.claude/commands/` failed to load because Claude Code cannot follow a symlink into a directory it hasn't been granted. The operator's global `~/.claude/settings.json` `additionalDirectories` entry for the workspace root was the only thing keeping things working, and it had proven unreliable in cascading to per-project sessions. The operator reported repeatedly hitting this with new-project-pipeline output.
- **Decision:** Reverse the earlier decision. `permissions.additionalDirectories` is now added per-project at post-pipeline enrichment time via a new numbered step 3 that walks upward from the project directory to locate the ancestor containing `ai-resources/`, then runs a separate idempotent `jq` merge on `.permissions.additionalDirectories`. The step is independent from step 2's canonical-permissions merge so that projects with pre-existing `permissions.allow` arrays (which skip the canonical merge via the existing predicate) still receive the grant. Retrofit applies to all 5 existing projects. `/repo-dd` gets Q3.5 + Q3.6 so any future regression is caught on audit.
- **Rationale:** Per-project entries are version-controlled, travel with the project (anyone else opening the project gets the grant without touching user-level settings), defend-in-depth against global-setting drift, and are auditable via the new questionnaire items. The absolute-path concern that originally motivated the exclusion is addressed by computing the path dynamically at enrichment time via an upward walk instead of hardcoding it into a canonical template. Skills under `ai-resources/skills/` are the load-bearing case: `auto-sync-shared.sh` only syncs commands and agents, so there is no symlink fallback for skills — `additionalDirectories` is the only mechanism that exposes them to a project session.
- **Alternatives considered:** (a) Launch-time `--add-dir ai-resources` via a README or launch script (rejected — relies on operator discipline, not version-controlled, easy to forget). (b) Rely solely on the operator's global `~/.claude/settings.json` entry (rejected — operator-global setting has proven unreliable in this workspace, and it isn't self-contained per project). (c) Relative path like `"../../ai-resources"` in the field (rejected — Claude Code resolves `additionalDirectories` relative to session CWD which varies by launch method, so relative paths can silently misresolve; the operator's global uses absolute form and we match that). (d) Bake `additionalDirectories` into the canonical CANONICAL_PERMS block in step 2 (rejected — the predicate at step 2 skips the merge entirely for projects that already have `permissions.allow` populated, which would leave those projects ungranted; a separate step 3 decouples the two concerns).

### 2026-04-13 — Commit Rules propagate by explicit copy to every project CLAUDE.md, not inheritance
- **Context:** The workspace-level `CLAUDE.md` at the Axcíon AI workspace root contains a canonical "Commit behavior" section stating "Commit directly. Do not ask for permission." Claude Code walks upward through parent directories when loading CLAUDE.md, so a project session *should* inherit that rule. In practice, this session demonstrated repeatedly that the inheritance path is silent and unreliable — the AI asked the operator for commit permission four times in one session despite the rule being in the parent CLAUDE.md, and the operator eventually escalated ("A MILLION TIMES"). Inheritance evidently does not surface the rule prominently enough to be load-bearing behavior.
- **Decision:** Copy a short `## Commit Rules` section into every project and template CLAUDE.md in the workspace, and add a Post-Pipeline Enrichment step to `/new-project` that creates or appends the same section to any new project's CLAUDE.md. The research-workflow template CLAUDE.md also receives the section so projects deployed via `/deploy-workflow` inherit it. The short form references the canonical workspace-level "Commit behavior" section as the source of truth, so canonical wording drift (if any) still has a single home in the workspace CLAUDE.md, and each project's short form is explicitly a mirror. Applied across 9 existing CLAUDE.md files this session; future projects pick it up automatically via the two pipeline paths.
- **Rationale:** Explicit beats implicit when the rule governs AI behavior that costs the operator real frustration every time it is ignored. Repeating the rule in every project CLAUDE.md guarantees that *whatever* CLAUDE.md loading order Claude Code uses, the rule is present in-context. The cost is a ~8-line duplication across ~10 files and a slight drift risk on the short-form wording, both acceptable. The alternative — debug Claude Code's CLAUDE.md loading order to make the parent rule more reliably visible — is unknowable from outside and fragile. Cost of being explicit is low; cost of guessing wrong is a whole category of operator-facing friction.
- **Alternatives considered:** (a) Only put the rule in the workspace-level CLAUDE.md and rely on inheritance (rejected — empirically failed this session). (b) Put the full canonical rule body in every project CLAUDE.md instead of a short-form reference (rejected — higher drift risk, and the short form is already sufficient because Claude can always re-read the canonical section). (c) Build a hook that injects the rule into every session transcript at load time (rejected — over-engineering for a problem solved by one-time file edits, and hooks run in a different context than CLAUDE.md). (d) Let new projects continue to depend on inheritance and only fix the *existing* projects (rejected — would leave the same hole open for every future project created via `/new-project` or `/deploy-workflow`).

### 2026-04-18 — Token-audit infrastructure uses the /repo-dd 3-file pattern, not the /audit-repo skill-package pattern
- **Context:** Operator authored a v1.1 Token Usage Optimization Audit Protocol (10 sections) and asked for matching infrastructure in `ai-resources/`. Initial plan proposed an 11-file skill package mirroring `repo-health-analyzer` (SKILL.md + command.md + references/ + agents/). Independent QC rejected that plan with 3 Critical + 6 Major findings — chief among them a `.claudeignore` silent-conflict resolution, per-file QC-framework scope mismatch (the eight-layer framework is skill-only but the plan committed to running it on 4 agent files + references), and ambiguity in slash-command placement between inline orchestration and skill-internal delegation.
- **Decision:** Pivot to the `/repo-dd` pattern: three files total — a single orchestrator slash command (`.claude/commands/token-audit.md`) with inline steps, a spec file (`audits/token-audit-protocol.md`) read at runtime, and a lean fresh-context subagent (`.claude/agents/token-audit-auditor.md`) for heavy-read Sections 2, 4, 5-conditional, 6. The command is analogous to `/repo-dd`'s inline orchestration (not `/audit-repo`'s thin-wrapper-to-skill style). The protocol file is analogous to `audits/questionnaire.md`. The subagent is analogous to `repo-dd-auditor.md`.
- **Rationale:** Diagnostic-only audits don't benefit from the skill-package overhead. The protocol IS the spec file, and `/repo-dd` already demonstrates the pattern works for multi-step inspection with subagent delegation. Matching the closer analog also avoids the skill-framework-vs-agent-file QC-scope mismatch that the initial plan generated. Fewer files (3 vs 11) also reduces the skill-index token cost — ironic for a token-reduction audit, but real: Option B (multiple independent skills) would add 6 new entries to every session's skill index.
- **Alternatives considered:** (a) Continue refining the 11-file skill-package (Option A, rejected after QC cycle 1). (b) Fold token-focus into `/repo-dd deep --token-focus` as a flag (Option C — would have reused `repo-dd-auditor` and eliminated overlap with `/repo-dd deep` Step 10's Context Load Assessment; rejected because token audits have a different cadence than repo-DD — periodic vs on-demand diligence — and overloading `/repo-dd` risks flag explosion). (c) Prompt file + thin command (Option D — simplest v1, rejected in favor of proper subagent isolation for Sections 2/4/6 which have filesystem-heavy reads). (d) Multiple independent skills per audit area (Option B; rejected because the audit's value is the integrated optimization plan in Section 9, and Option B adds 6 entries to every session's skill index).

### 2026-04-18 — Token-audit protocol v1.2 checks for Read(pattern) deny rules specifically, not generic permissions.deny
- **Context:** Operator's v1.1 protocol checked for a `.claudeignore` file at the repo root and treated its absence as a HIGH finding. Research via `claude-code-guide` subagent confirmed `.claudeignore` is not a documented Claude Code feature — it exists in Cursor/Aider, not Claude Code. Initial plan proposed substituting "`.claudeignore` present" with "`permissions.deny` present" in the protocol. Independent QC flagged (C-NEW-1) that this substitution was too loose: `permissions.deny` is currently used in the Axcíon workspace for action-prevention (`Write(...)`, `Bash(...)`), which does not answer the context-load question that `.claudeignore` in other tools answers. Only `Read(pattern)` deny rules actually prevent file-content loading into Claude's context (they block file discovery and reading across Glob, Grep, Read, and Edit tools).
- **Decision:** v1.2 of the protocol checks specifically for `permissions.deny` entries matching the `Read(...)` pattern, filters the rest, and compares coverage against an expected-coverage list of stale/large directories (`audits/`, `logs/`, `reports/`, `inbox/`, `archive/`, `output/`, `drafts/`, and directories containing `deprecated` or `old`). Severity: HIGH if no `Read(...)` denies exist; MEDIUM if coverage missing for >2 expected directories; PASS otherwise. Corrections applied consistently across Sections 0.3, 6.2, and 7. v1.2 frontmatter records the correction list so future readers can see what changed from v1.1.
- **Rationale:** Substituting one feature for another silently ships authoritative-looking findings about the wrong mechanism. Making the check specific to `Read(...)` denies measures what the original protocol was trying to measure — whether stale directories are excluded from Claude's context — and ties severity to coverage completeness rather than raw presence. The expected-coverage list is derived from common stale-content patterns in AI-workspace repos.
- **Alternatives considered:** (a) Drop the check entirely (rejected — the underlying concern about unchecked file reads is real). (b) Keep the v1.1 protocol verbatim with a sidecar `references/claudeignore-note.md` (rejected per workspace CLAUDE.md "Conflicts must be surfaced, not silently resolved" — a sidecar does not reliably reach a subagent executing the main protocol instruction). (c) Runtime-map `.claudeignore` to `permissions.deny` in the skill body while preserving the protocol text (rejected as a disguised silent resolution). (d) Substitute to generic `permissions.deny` (the original plan, rejected per QC C-NEW-1).

