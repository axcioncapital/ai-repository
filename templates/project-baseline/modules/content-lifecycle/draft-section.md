---
friction-log: true
---
Draft content for: $ARGUMENTS

Before writing — always read these:
1. Read the relevant content specification for this section
2. Read the style guide (if one exists) for tone and formatting
3. Check the relevant `drafts/` directory for existing draft iterations
4. Verify development sequencing: if this section depends on upstream sections, confirm those have approved content before proceeding. If dependencies aren't met, flag the dependency and ask {{OPERATOR_NAME}} how to proceed.

If any reference file is empty or contains only a placeholder, note which files were unavailable and proceed with the context you have.

Read these during drafting as needed — do not front-load:
- Project brief — if you need to check strategic alignment
- Glossary — if you encounter domain-specific terminology
- Other sections' `approved/` directories — if the section has cross-references

If previous drafts exist for this section, read the most recent one and identify
what needs to change or continue from where it left off.

Use plan mode: present your approach (structure, key points, sources you'll draw from)
before writing. Wait for {{OPERATOR_NAME}}'s approval of the plan before drafting.

Save progress incrementally: after completing each major section of the draft, write the current state to the drafts/ directory as a work-in-progress file.

Save the draft to the appropriate `drafts/` directory with the next
iteration number (e.g., if draft-02.md exists, create draft-03.md).

After drafting, ask if {{OPERATOR_NAME}} wants to:
- Iterate on specific sections
- Run a QC review on the draft
- Move on to something else
