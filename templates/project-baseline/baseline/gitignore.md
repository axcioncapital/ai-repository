# Standard .gitignore for Axcíon projects

Content to write as the project's `.gitignore` file:

```
.DS_Store
.claude/settings.local.json
```

Notes:
- `.DS_Store` — macOS system files
- `.claude/settings.local.json` — local model overrides and personal settings (not shared via git)
