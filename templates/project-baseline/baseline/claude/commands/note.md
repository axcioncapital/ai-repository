Append a workflow observation to `/logs/workflow-observations.md`.

Format: `YYYY-MM-DD HH:MM — $ARGUMENTS`

If the file doesn't exist, create it with a `# Workflow Observations` header first.

Do not ask for confirmation — just append and confirm it was written.
